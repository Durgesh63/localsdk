# Development dependencies for working on the SDK itself.
#   pip install -r requirements-dev.txt
#   pip install -e .          # then install the package in editable mode

-r requirements.txt

pytest>=7.4          # tested with 9.0.2
pytest-asyncio>=0.23 # tested with 1.3.0
anyio>=4             # tested with 4.13.0
