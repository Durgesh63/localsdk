# Runtime dependencies for the localsdk client package.
#
# Normally you do NOT need this file - installing the package pulls these in:
#   pip install "git+https://github.com/Durgesh63/localsdk.git#subdirectory=sdk"
#
# It exists for environments that install from a requirements file by policy.
# pyproject.toml is authoritative.

httpx>=0.27          # tested with 0.28.1
pydantic>=2          # tested with 2.12.4
tomli>=2.0; python_version < '3.11'   # config-file support on Python 3.10 only
