# Development dependencies: everything in requirements.txt plus the test tools.
#   pip install -r requirements-dev.txt

-r requirements.txt

pytest>=7.4          # tested with 9.0.2
pytest-asyncio>=0.23 # tested with 1.3.0
anyio>=4             # tested with 4.13.0
