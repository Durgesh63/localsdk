# Runtime dependencies for the LocalSDK server.
#
# pyproject.toml is the authoritative source; this file exists for the common
# `pip install -r requirements.txt` workflow and must be kept in step with it.
#
# Lower bounds, not pins: these are the versions the suite is known to pass on.
# Pin exactly (==) if you need byte-identical deployments.

fastapi>=0.115            # tested with 0.121.0
uvicorn[standard]>=0.30   # tested with 0.38.0
httpx>=0.27               # tested with 0.28.1
pydantic>=2               # tested with 2.12.4
pydantic-settings>=2      # tested with 2.15.0
python-dotenv>=1          # tested with 1.2.1
